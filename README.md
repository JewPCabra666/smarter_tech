# Smarter Tech Interview

Small Python exercise that classifies packages into handling stacks based on size and weight.

## Requirements

- Python 3.11+

## Run Instructions

Just run `python main.py` or `uv run main.py`